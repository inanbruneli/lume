{
  "name": "lume",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,cyan \"npm -w apps/api run dev\" \"npm -w apps/web run dev\"",
    "build": "npm -w packages/shared run build && npm -w apps/api run build && npm -w apps/web run build"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "typescript": "^7.0.2"
  }
}
